def matrix_summe_allgemein(mymatrix):
        sum=0
        for i in range(len(mymatrix)):
                for j in range(len(mymatrix[0])):
			if mymatrix[i][j]<mymatrix[i]:
				def break_func(mymatrix[i][j])
                        sum+=mymatrix[i][j]
        return sum
def break_func(mymatrix[i][j]):
	len(matrix
	
example=[
        [1,2,3],
        [4,5,6],
        [7,8,9]
]
print(matrix_summe_allgemein(example))
